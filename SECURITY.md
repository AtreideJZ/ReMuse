# 安全政策

## 报告漏洞

**请不要在公开 issue 中报告安全漏洞。**

推荐渠道：本仓库 GitHub 页面 → **Security** 标签 → **Report a vulnerability**（私密报告，仅维护者可见）。

请附上：影响范围、复现步骤、你认为的利用前提。维护者会尽快确认收到，但不承诺具体修复时限（本项目为单人维护）。

## 支持版本

仅最新发布版本（`v0.x` 最新 tag）接受安全修复。`0.x` 阶段接口可能变更，请保持更新。

## 部署形态与责任边界

ReMuse 是**自部署单用户**应用，安全模型的前提是：数据属于部署者自己，管理面由部署者独占。以下事项属于**部署者责任**，请务必做到：

- `ADMIN_TOKEN` 与 `POSTGRES_PASSWORD` 使用高强度随机值（生成方式见 `.env.example` 注释）
- 数据库端口已默认仅绑定 `127.0.0.1`——**不要**改动这一映射
- 经 Cloudflare Tunnel / VPS 反代等方式把服务暴露到公网时，自行配置 TLS，并置 `ENABLE_HSTS=true`
- 定期备份（`scripts/backup.sh`）并自行验证可恢复

## 当前的安全设计

只描述现状，供审计与部署决策参考：

- **管理面认证 fail-closed**：所有 Web 管理端点要求 `ADMIN_TOKEN`；未配置时管理面整体返回 503，而不是放行
- **Agent 默认只读**：API Key 默认仅只读 scope，写入需显式授权；可按项目空间限定范围
- **吊销即失效**：每次请求都查库校验，无缓存
- **密钥不落明文**：API Key 数据库存 SHA-256 哈希，完整密钥仅创建时返回一次
- **全量审计**：MCP 与 REST Agent 接口每次调用（含越权尝试）均写 `agent_call_logs`，默认保留 90 天
- **灵感原文不可变**：由数据库触发器强制
- **原文视为不可信输入**：返回给 Agent 的内容带隔离标记；LLM 结构化时原文以分隔符包裹并声明不可信（防 Prompt Injection）
- **限流**：写入 / 检索 / 导出端点滑窗限流，防计费放大与 DoS
- **SSRF 防护**：`LLM_BASE_URL` / `EMBEDDING_BASE_URL` 启动时校验 scheme，默认拒绝私网/回环地址
- **安全响应头**：API 与 Web 均配置（含 CSP、HSTS 开关）
- **容器非 root 运行**；数据库不出内网
- **Agent 无删除能力**：任何 Agent 都无法删除或改写灵感

## 注意：返回给 Agent 的内容不可信

灵感库中的内容是用户随手记录的文本，可能包含任何形式的指令样式文本。接入 Agent 时请确保你的 Agent 平台妥善处理工具返回内容中的潜在指令（Prompt Injection），ReMuse 侧已做标记隔离，但不能替代 Agent 平台的处理。
