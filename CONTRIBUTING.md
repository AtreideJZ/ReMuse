# 贡献指南

感谢你愿意为 ReMuse 溯游贡献。为了让双方的投入都不被浪费，请先读完这份文档再动手——尤其是「方向边界」一节，它决定什么样的 PR 会被接受。

## 项目现状

本项目当前为**单人维护**，处于复用率验证阶段。这意味着：

- **不承诺任何议题的响应时间**——这比承诺了做不到要好得多
- 优先处理影响核心闭环（记录 → 找回 → 复用）的问题
- 重复追问不会改变优先级

## 方向边界

写在这里，是为了让「能不能加 XX」有一个统一的答案，而不必每次现场解释。与 README 的「设计取舍」一节保持一致。

### 永久不做（产品定位，需求再高也不改变）

| 边界 | 理由（可核查的事实） |
|------|----------------------|
| 多人协作 / 团队共享 | ReMuse 是**个人工具**。认证层只有一个管理令牌，数据模型里没有「用户」概念——加进来是架构级变更，不是加一个开关 |
| 企业版功能（SSO / RBAC / 审计） | 依赖上一条，同样不纳入 |

### 当前阶段不做，但不排除（瓶颈在开发资源，不在需求）

| 边界 | 说明 |
|------|------|
| 原生 App | 单人维护难以同时支撑 Web、后端与两个平台的原生客户端。PWA 已覆盖手机记录主干动线（装主屏 / 离线记录 / 分享目标）。**解锁条件不是「呼声够高」，而是有人能承担开发**——若你有意愿，欢迎开 issue 讨论 |

### 当前阶段不做，且不接受实现类 PR（有明确触发条件）

| 边界 | 说明 |
|------|------|
| 云托管版 | 只做自部署。托管版的启动条件有明确定义（复用率验证通过），不满足就不会启动 |

护栏：以上边界**不承诺时间表，不因重复询问而调整优先级**。对「不排除」的条目，其判定标准是可证伪的（有人承担开发 / 触发条件达成），不是 +1 的数量。

## 议题（Issue）规则

多数议题会是**各 Agent 平台的接入问题**（MCP 配置、Bearer Token、连接失败）。issue 模板会**强制要求填写 Agent 平台名称与版本**，请完整填写——缺了它，分诊无法进行。

分诊按可复现性优先：

1. 无复现步骤或环境信息的议题会被标记 `needs-repro` 并追问
2. 超期无响应的议题会被关闭，**补充信息即可随时重开**——关闭不是拒绝，是排队机制
3. 有复现步骤的优先处理

标签体系：`bug` / `needs-repro` / `good-first-issue` / `help-wanted` / `wontfix` / `docs`。第一次贡献建议从议题列表中筛 `good-first-issue` 标签开始。

## 提交规范

- **一个 PR 只做一件事**。混合多个改动的 PR 会被要求拆开
- 提交信息使用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/)：`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:` 等
- 分支：从 `main` 拉出功能分支（如 `fix/xxx`、`feat/xxx`），合并回 `main`

## 本地开发与验证

环境搭建见 README「本地开发」一节。提交前请确保：

```bash
# 后端（需要数据库在运行，见 README）
cd backend && .venv/Scripts/python -m pytest tests/ -q   # Windows Git Bash
# macOS / Linux 用 .venv/bin/python

# 前端
cd frontend && npm run lint
```

CI 门禁（PR 必须全绿）：后端 pytest 全量、前端 ESLint 与 `tsc --noEmit`。

改动以下任一契约时，PR 中必须附带对应的迁移/兼容说明：MCP 工具签名、`/api/agent/*` 接口、数据库 schema（Alembic 迁移）。

## 评审标准

- CI 全绿是前提
- 优先覆盖边界与安全场景（scope、越权、降级）——本项目是单用户自部署工具，安全红线见 `AGENTS.md` 的设计要点
- 遵循最小变更原则：不做附带重构或重命名
- 评审与合并由维护者进行，无固定时长承诺

## 许可与权利声明

- 本项目以 **Apache License 2.0** 发布。依据该许可证第 5 条：除非你明确另行声明，**任何有意提交以纳入本作品的贡献，均默认按 Apache License 2.0 的条款授权**（即 DCO 式安排，无需签署任何文件）
- 依据该许可证第 4(b) 条：**提交即表示你同意在修改过的文件中保留或添加显著的修改说明**
- 请只提交你有权贡献的内容

## 安全漏洞

**不要**在公开 issue 中报告安全漏洞，披露渠道见 [SECURITY.md](SECURITY.md)。
