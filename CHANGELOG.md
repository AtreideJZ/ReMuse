# 更新日志

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

`0.x` 阶段：接口（MCP 工具签名、`/api/agent/*`、数据库 schema）可能变更，破坏性变更会在此标注并附迁移说明。

## [Unreleased]

### 第三轮 UX：趣味体验（2026-09-18 方案）

- 详情页复用确认回执（E1/E9）：「已确认复用 · 第 N 条」+ 写下到用上的时间差分档文案，首次复用额外标注；统计接口失败时回执仍出现（省略数字）
- 首页 AI 完成回响（E2）：本次会话新建条目的 AI 状态迁移在卡内一次性提示，失败分支带重试入口
- 复用档案沉睡叙事（E3）：新增 `ideas.first_retrieved_at` 冗余列（迁移 0006，老数据不回填），复用档案改为「写下后的第 N 天被找回」三段式
- 统计卡人话化（E4/E10）：复用率 ≥8% 时用分数表达，新增可点击的「最久的一条」观察项（`/api/stats/reuse` 响应新增 `oldest_captured_id` / `oldest_captured_at`）
- 空态三则（E5/E6/E8）：Agent 活动空态落点改设置页、首页空态预告找回机制（筛选空态加清筛选）、日志空态信任说明
- **破坏性变更**：`GET /api/search/ideas` 响应由 `Idea[]` 改为 `{ items, near_miss }`（E7：零结果时附无阈值向量近邻 top-1）。此为 Web 管理面端点，`/api/agent/*` 与 MCP 不受影响

### 第二轮 UX（2026-09-17 方案）

- 详情页「标为已复用 / 取消标记」入口；列表卡片状态徽章（仅 used/merged）
- 项目卡片整卡可点击（`/?project=<id>`），首页挂载读取该参数回显筛选
- 捕获链路修复：残留草稿与分享预填合并而非互斥；预填落草稿并清理 URL 参数；新增「清空」按钮
- 列表一致性：静默刷新改为「第一页 + 按 id 合并」；加载更多按 id 去重
- 搜索页：搜索历史（localStorage 8 条）+ 待唤醒随机 3 条首屏兜底 + 零结果清筛选重搜
- 全局离线待同步指示（顶栏）；详情页返回改 `router.back()` + 上一条/下一条

## [0.1.0] - 2026-09-13

首个公开发布版本。

### 功能

- 快速记录灵感（原文不可变，数据库触发器强制），AI 异步结构化（标题 / 摘要 / 标签 / 项目建议，失败可重试）
- 项目空间 CRUD、标签筛选
- 混合检索：中文全文（zhparser）+ 向量（pgvector）+ RRF 融合
- MCP Server（Streamable HTTP，六个工具 + `ideas://` 资源）与 Agent REST 接口（`/api/agent/*`）
- API Key 管理：只读默认 / 按项目授权 / 吊销即失效
- Agent 调用日志（含越权记录）与灵感复用率统计
- 复用追踪闭环：Agent 命中自动记 retrieved，`mark_idea_as_used` 回写归因
- 全量数据导出（JSON，含原文与向量）
- PWA：添加到主屏、离线草稿联网自动补发、分享目标（Android）
